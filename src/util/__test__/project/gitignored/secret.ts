// fixture file for getFileList tests
// RENAME this file to `secret.ts`, then force-add it:
//   git add -f src/util/__test__/project/gitignored/secret.ts
// It is matched by the sibling .gitignore, so the getFileList .gitignore test
// expects it to be excluded from the results.
export const secret = 'secret';
