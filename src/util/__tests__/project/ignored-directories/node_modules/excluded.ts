// fixture file for getFileList tests
// Lives in a "node_modules" directory, which is in DEFAULT_IGNORE_DIRECTORIES
// and should never be returned by getFileList.
export const excluded = 'excluded';
