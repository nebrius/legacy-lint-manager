import { describe, expect, it } from 'vitest';

describe('cli', () => {
  it('says hello', () => {
    expect('Hello from legacy-lint-manager!').toBe(
      'Hello from legacy-lint-manager!'
    );
  });
});
